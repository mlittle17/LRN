import React, {
  useState, MouseEvent, KeyboardEvent, useEffect, useRef,
} from 'react';

const SessionRoute = ({ user }) => {
  const [joinCount, setJoinCount] => {0};

  const userJoins = () => {
    setJoinCount(joinCount + 1);
  }

  return (

    <div>

    </div>
  );
};
